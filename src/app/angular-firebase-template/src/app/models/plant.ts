export class Plant {
    id: string;
    name: string;
    species: string;

    constructor(id: string, name: string, species: string) {
        this.id = id;
        this.name = name;
        this.species = species;
    }
}